# Public (inga riktiga hemlisar finns fär)
# Exempel: your public API base (must start with NEXT_PUBLIC_)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api
NEXT_PUBLIC_APP_NAME=Student Event

# Server-only (Hemnlig information - inte dela!!)
# Exempel: DB connection string
DATABASE_URL=

# third-party keys (bara för servern)
API_SECRET_KEY=

# Next.js config toggles
NODE_ENV=development